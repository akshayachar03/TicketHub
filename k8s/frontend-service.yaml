apiVersion: v1
kind: Service

metadata:
  name: tickethub-frontend-service
  namespace: tickethub

spec:
  type: ClusterIP

  selector:
    app: tickethub-frontend

  ports:
    - port: 80
      targetPort: 80